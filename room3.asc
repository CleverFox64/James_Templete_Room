// room script file

function hHotspot1_WalkOn(Hotspot *theHotspot)
{
  player.ChangeRoom(4, 195, 80, eDirectionDown);
}

function room_Load()
{
  //player.ChangeView(5);
  //player.Animate(0, 3, eOnce, eBlock);
  
  
  //Trying to make the Faucet Drip forever when the room starts
  //FaucetDripping
  //oFaucetDripping.Loop = true;
  //oFaucetDripping.ChangeView(6);
  //oFaucetDripping.Animate(6, 2, eRepeat, eNoBlock);
}

function oFaucetDripping_Interact(Object *theObject, CursorMode mode)
{
  //When the player interacts with the Faucet with a Bucket they will place the bucket under it
  //Then a view will show that a bucket is being filled and the nthe players gets it as a INV item "Filled Buket"
  
  //CODE HERE
}
